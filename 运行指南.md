# 跨时空邮局网站运行指南

## 📋 前置要求

### 1. 安装 Node.js
- 确保已安装 Node.js（建议版本 16+）
- 检查版本：`node -v` 和 `npm -v`

### 2. 安装 MySQL
- 确保 MySQL 已安装并运行
- 数据库名称：`cross_time_post`
- 用户名：`root`
- 密码：`123456`
- 端口：`3306`

### 3. 创建数据库
确保已执行数据库建表语句，创建所有必要的表。

---

## 🚀 运行步骤

### 第一步：配置环境变量

在项目根目录创建 `.env` 文件：

```bash
# 在项目根目录执行（Windows PowerShell）
Copy-Item .env.example .env
```

或者手动创建 `.env` 文件，内容如下：

```env
# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=cross_time_post

# JWT密钥
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRE=7d

# 服务器配置
PORT=3000
NODE_ENV=development

# AI API配置（可选，用于生成回信）
AI_API_KEY=your-ai-api-key
AI_API_URL=https://api.openai.com/v1/chat/completions
```

---

### 第二步：安装后端依赖

在项目根目录执行：

```bash
npm install
```

---

### 第三步：初始化数据（可选但推荐）

```bash
# 初始化示例历史人物和样式配置
npm run init-data

# 创建管理员账户
npm run create-admin
```

创建管理员时会提示输入：
- 用户名（例如：admin）
- 密码（例如：admin123）
- 邮箱（例如：admin@example.com）
- 角色（super_admin 或 content_admin）

---

### 第四步：启动后端服务器

```bash
# 开发模式（自动重启）
npm run dev


# 或生产模式
npm start
```

后端服务将运行在：**http://localhost:3000**

看到以下信息表示启动成功：
```
数据库连接成功
服务器运行在端口 3000
```

---

### 第五步：安装前端依赖

打开新的终端窗口，进入前端目录：

```bash
cd frontend
npm install
```

如果遇到版本冲突，可以尝试：
```bash
npm install --legacy-peer-deps
```

---

### 第六步：启动前端开发服务器

在 `frontend` 目录下执行：

```bash
npm run dev
```

前端服务将运行在：**http://localhost:5173**

---

## 🌐 访问网站

### 用户端
- **首页**：http://localhost:5173
- **登录页**：http://localhost:5173/login
- **注册页**：http://localhost:5173/register

### 管理员端
- **管理员登录**：http://localhost:5173/admin/login
- **管理后台**：http://localhost:5173/admin（需要先登录）

---

## 📝 快速测试流程

### 1. 测试用户功能

1. 访问 http://localhost:5173
2. 点击"注册"创建账号
3. 登录后可以：
   - 查看精选信件
   - 写信给历史人物
   - 查看我的信件
   - 生成AI回信

### 2. 测试管理员功能

1. 访问 http://localhost:5173/admin/login
2. 使用创建的管理员账号登录
3. 可以：
   - 查看数据统计
   - 管理用户
   - 管理信件（设置精选）
   - 管理历史人物

---

## ⚠️ 常见问题

### 1. 数据库连接失败

**错误信息**：`数据库连接失败`

**解决方法**：
- 检查 MySQL 服务是否运行
- 检查 `.env` 文件中的数据库配置是否正确
- 确认数据库 `cross_time_post` 已创建
- 确认用户名、密码、端口号正确

### 2. 端口被占用

**错误信息**：`Port 3000 is already in use`

**解决方法**：
- 修改 `.env` 文件中的 `PORT` 值
- 或关闭占用端口的程序

### 3. 前端无法连接后端

**解决方法**：
- 确认后端服务已启动（http://localhost:3000）
- 检查 `frontend/vite.config.js` 中的代理配置
- 确认后端服务健康检查：访问 http://localhost:3000/health

### 4. npm install 失败

**解决方法**：
```bash
# 清除缓存
npm cache clean --force

# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install

# 如果还有问题，使用
npm install --legacy-peer-deps
```

---

## 🔧 开发模式 vs 生产模式

### 开发模式
- **后端**：`npm run dev`（使用 nodemon，代码修改自动重启）
- **前端**：`npm run dev`（使用 Vite，热更新）

### 生产模式
- **后端**：`npm start`（使用 node）
- **前端**： 
  ```bash
  npm run build    # 构建生产版本
  npm run preview  # 预览构建结果
  ```

---

## 📊 项目结构

```
jikedao/
├── config/              # 后端配置
│   └── database.js      # 数据库配置
├── middleware/          # 后端中间件
├── routes/              # 后端路由
├── scripts/             # 脚本工具
├── server.js            # 后端入口
├── package.json         # 后端依赖
├── .env                 # 环境变量（需创建）
└── frontend/            # 前端项目
    ├── src/
    │   ├── views/       # 页面组件
    │   ├── router/      # 路由配置
    │   ├── stores/       # 状态管理
    │   └── utils/       # 工具函数
    ├── package.json     # 前端依赖
    └── vite.config.js   # Vite配置
```

---

## 🎯 完整启动命令总结

### 方式一：分步启动（推荐）

**终端1 - 后端**：
```bash
# 在项目根目录
npm install
npm run init-data
npm run create-admin
npm run dev
```

**终端2 - 前端**：
```bash
# 在 frontend 目录
cd frontend
npm install
npm run dev
```

### 方式二：使用脚本（可自行创建）

可以创建一个启动脚本 `start.bat`（Windows）或 `start.sh`（Linux/Mac）：

**Windows (start.bat)**:
```batch
@echo off
echo 启动后端服务器...
start cmd /k "npm run dev"
timeout /t 3
echo 启动前端服务器...
start cmd /k "cd frontend && npm run dev"
```

---

## ✅ 验证运行状态

### 检查后端
访问：http://localhost:3000/health

应该返回：
```json
{
  "status": "ok",
  "message": "服务器运行正常"
}
```

### 检查前端
访问：http://localhost:5173

应该看到网站首页。

---

## 🔐 默认配置

- **后端端口**：3000
- **前端端口**：5173
- **数据库**：MySQL (localhost:3306)
- **数据库名**：cross_time_post
- **数据库用户**：root
- **数据库密码**：123456

---

## 📞 需要帮助？

如果遇到问题：
1. 检查终端错误信息
2. 确认所有依赖已安装
3. 确认数据库服务运行正常
4. 检查端口是否被占用
5. 查看 README.md 获取更多信息

