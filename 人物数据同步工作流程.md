# 人物数据同步工作流程

## ⚠️ 重要提醒

**通过管理员后台添加的人物数据存储在数据库中，Git推送代码不会同步数据库数据！**

必须通过导出/导入的方式同步人物数据。

---

## 📋 标准工作流程

### 场景：你通过管理员后台添加了新人物，需要同步给协作者

### 步骤一：添加人物（你的环境）

1. 登录管理员后台：`http://localhost:5173/admin/login`
2. 进入"历史人物管理"
3. 添加新人物并保存

### 步骤二：导出数据（你的环境）

运行同步脚本：

```bash
npm run sync-figures
```

或者手动导出：

```bash
npm run export-figures
```

这会生成 `figures-export.json` 文件。

### 步骤三：提交到Git（你的环境）

```bash
git add figures-export.json
git commit -m "添加新人物：XXX、YYY等"
git push
```

### 步骤四：协作者同步（协作者环境）

协作者拉取代码后：

```bash
git pull
npm run import-figures
```

完成！协作者现在可以看到所有新添加的人物了。

---

## 🔄 完整示例

### 示例：添加了3个新人物

**你的操作：**

```bash
# 1. 通过管理员后台添加了3个人物
# 2. 导出数据
npm run sync-figures

# 输出：
# ✅ 导出成功！
# 📁 文件位置：D:\VS Code\jikedao\figures-export.json
# 📊 导出数量：10 个

# 3. 提交到Git
git add figures-export.json
git commit -m "添加新人物：孙悟空、哈利波特、哆啦A梦"
git push
```

**协作者操作：**

```bash
# 1. 拉取代码
git pull

# 2. 导入人物数据
npm run import-figures

# 输出：
# ✅ 已添加：孙悟空（神话）
# ✅ 已添加：哈利·波特（现代）
# ✅ 已添加：哆啦A梦（未来）
# ...
# 完成！
# ✅ 成功添加：3 个
# 🔄 已更新：0 个
```

---

## 💡 最佳实践

1. **定期同步**
   - 每次添加人物后，立即导出并提交到Git
   - 避免积累太多人物后一次性同步

2. **提交信息**
   - 在commit信息中说明添加了哪些人物
   - 例如：`git commit -m "添加新人物：孙悟空、哈利波特"`

3. **协作者提醒**
   - 推送后通知协作者拉取并导入
   - 或者在团队群中说明

4. **版本控制**
   - `figures-export.json` 文件会被Git管理
   - 可以查看历史版本，了解人物数据的变化

---

## 🚨 常见问题

**Q: 为什么Git推送后协作者看不到新人物？**

A: 因为人物数据存储在数据库中，不在代码里。必须：
1. 导出数据文件（`figures-export.json`）
2. 提交文件到Git
3. 协作者拉取后导入

**Q: 可以删除 `figures-export.json` 文件吗？**

A: 可以，但建议保留。这个文件是人物数据的备份，方便协作者同步。

**Q: 如果协作者也添加了人物怎么办？**

A: 导入脚本会自动处理：
- 如果人物不存在，会添加
- 如果人物已存在（同名同时代），会更新
- 不会删除已有的人物

**Q: 如何避免冲突？**

A: 建议：
- 添加人物前先拉取最新代码
- 添加后立即导出并提交
- 团队内协调，避免同时添加相同人物

---

## 📝 相关命令

```bash
# 导出人物数据
npm run export-figures

# 导入人物数据
npm run import-figures

# 同步人物数据（导出+提示）
npm run sync-figures

# 批量添加人物（脚本方式）
npm run add-figures
```

---

## 📁 相关文件

- `figures-export.json` - 人物数据导出文件（需提交到Git）
- `scripts/export-figures.js` - 导出脚本
- `scripts/import-figures.js` - 导入脚本
- `scripts/sync-figures.js` - 同步脚本

