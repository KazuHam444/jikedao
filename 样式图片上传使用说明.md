# 样式图片上传使用说明

## 📸 功能说明

现在你可以上传自己的信纸、字体和边框样式图片，并在写信时选择使用！

## 🎯 使用步骤

### 1. 管理员上传样式图片

1. **登录管理员后台**
   - 访问 `/admin/login` 登录管理员账号

2. **进入样式管理**
   - 在左侧菜单点击"样式管理"
   - 选择要管理的样式类型（信纸样式/字体样式/边框样式）

3. **添加新样式**
   - 点击"添加样式"按钮
   - 填写样式信息：
     - **样式类型**：选择信纸/字体/边框
     - **样式名称**：给样式起个名字（如：古典信纸、楷体字体等）
     - **样式值**：用于代码识别（如：classic、kaiti等，建议使用英文）
     - **预览图片**：点击"上传图片"按钮，选择你的图片文件

4. **上传图片要求**
   - 支持格式：jpg、png、gif、webp
   - 文件大小：不超过5MB
   - 建议尺寸：
     - 信纸样式：800x600 或更大
     - 字体样式：400x300 或更大
     - 边框样式：400x300 或更大

5. **保存样式**
   - 上传图片后，会显示预览
   - 点击"保存"按钮完成添加

### 2. 用户使用样式

1. **写信时选择样式**
   - 进入写信页面
   - 在"信纸样式"、"字体样式"、"边框样式"下拉框中
   - **可以看到每个样式的预览图片**
   - 选择你喜欢的样式

2. **预览效果**
   - 填写信件内容后
   - 点击"预览信纸"按钮
   - 查看最终效果

3. **发送信件**
   - 确认样式后点击"确认并发送"
   - 信件会以你选择的样式显示

## 📁 图片存储位置

上传的图片存储在：
```
项目根目录/uploads/styles/
```

图片文件名格式：`style-时间戳-随机数.扩展名`

## 🔧 技术说明

### 后端API

- **上传图片**：`POST /api/upload/style-image`
  - 需要管理员权限
  - 使用multipart/form-data格式
  - 字段名：`image`

- **创建样式**：`POST /api/admin/styles`
  - 需要管理员权限
  - Body: `{ style_type, style_name, style_value, preview_url }`

- **更新样式**：`PUT /api/admin/styles/:styleId`
  - 需要管理员权限
  - Body: `{ style_name?, style_value?, preview_url?, is_active? }`

### 前端组件

- **样式管理页面**：`frontend/src/views/admin/Styles.vue`
- **样式选择**：`frontend/src/views/Write.vue`
- **样式显示**：`frontend/src/components/LetterDisplay.vue`
- **样式预览**：`frontend/src/components/LetterPreview.vue`

## 💡 使用建议

1. **信纸样式图片**
   - 建议使用淡雅的背景色
   - 可以添加纹理效果
   - 确保文字在上面清晰可读

2. **字体样式图片**
   - 可以展示不同字体的效果
   - 建议使用文字示例图

3. **边框样式图片**
   - 展示边框的视觉效果
   - 可以是边框的截图或示例图

## ⚠️ 注意事项

1. **图片路径**
   - 上传的图片会保存在服务器本地
   - 图片URL格式：`/uploads/styles/文件名`
   - 前端会自动添加API基础URL

2. **图片访问**
   - 后端已配置静态文件服务
   - 图片可以通过 `http://localhost:3000/uploads/styles/文件名` 访问

3. **样式应用**
   - 如果样式有预览图片，会优先使用预览图片
   - 如果没有预览图片，会使用默认的CSS样式

## 🐛 常见问题

### Q: 上传图片后看不到预览？
A: 检查图片URL是否正确，确保后端服务正在运行，图片文件已保存到 `uploads/styles/` 目录。

### Q: 样式选择下拉框看不到图片？
A: 确保样式配置中有 `preview_url` 字段，并且图片URL可以正常访问。

### Q: 信件显示时样式不对？
A: 检查 `LetterDisplay` 组件是否正确获取了样式配置，查看浏览器控制台的调试信息。

## 📝 示例

### 添加一个古典信纸样式

1. 样式类型：`paper`
2. 样式名称：`古典信纸`
3. 样式值：`classic_paper`
4. 预览图片：上传一张古典风格的信纸图片

保存后，用户在写信时就可以在下拉框中看到这个样式，并选择使用！

