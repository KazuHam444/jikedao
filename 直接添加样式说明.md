# 直接添加样式配置说明

本文档说明如何不通过管理员界面，直接在数据库或后端添加样式配置。

## 📋 方法一：使用脚本添加（推荐）

### 1. 准备图片文件

将你的样式图片放在项目目录下的任意位置，例如：
```
项目根目录/
├── images/
│   ├── classic-paper.jpg      # 古典信纸
│   ├── kaiti-font.jpg         # 楷体字体
│   └── classic-border.jpg     # 古典边框
```

### 2. 运行添加脚本

```bash
# 添加信纸样式
node scripts/add-style.js paper "古典信纸" classic ./images/classic-paper.jpg

# 添加字体样式
node scripts/add-style.js font "楷体字体" kaiti ./images/kaiti-font.jpg

# 添加边框样式
node scripts/add-style.js border "古典边框" classic ./images/classic-border.jpg

# 不添加图片（只添加样式配置）
node scripts/add-style.js paper "默认信纸" default
```

### 3. 脚本参数说明

```
node scripts/add-style.js <style_type> <style_name> <style_value> [preview_image_path]
```

- **style_type**: 样式类型，必须是 `paper`、`font` 或 `border`
- **style_name**: 样式名称，显示给用户的名称（如："古典信纸"）
- **style_value**: 样式值，用于代码识别（如：`classic`、`kaiti`）
- **preview_image_path**: 预览图片路径（可选），相对路径或绝对路径

### 4. 脚本功能

- ✅ 自动验证样式类型和图片格式
- ✅ 自动复制图片到 `uploads/styles/` 目录
- ✅ 自动生成唯一文件名
- ✅ 如果样式值已存在，会更新现有记录
- ✅ 如果样式值不存在，会创建新记录

---

## 📋 方法二：直接在数据库中添加

### 1. 准备图片文件

将图片文件复制到 `uploads/styles/` 目录，例如：
```
uploads/styles/
└── classic-paper.jpg
```

### 2. 连接数据库

使用 MySQL 客户端连接数据库：
```bash
mysql -u root -p cross_time_post
```

### 3. 插入样式记录

```sql
-- 添加信纸样式（带图片）
INSERT INTO style_configs (style_type, style_name, style_value, preview_url, is_active)
VALUES ('paper', '古典信纸', 'classic', '/uploads/styles/classic-paper.jpg', 1);

-- 添加字体样式（带图片）
INSERT INTO style_configs (style_type, style_name, style_value, preview_url, is_active)
VALUES ('font', '楷体字体', 'kaiti', '/uploads/styles/kaiti-font.jpg', 1);

-- 添加边框样式（带图片）
INSERT INTO style_configs (style_type, style_name, style_value, preview_url, is_active)
VALUES ('border', '古典边框', 'classic', '/uploads/styles/classic-border.jpg', 1);

-- 添加样式（不带图片）
INSERT INTO style_configs (style_type, style_name, style_value, preview_url, is_active)
VALUES ('paper', '默认信纸', 'default', NULL, 1);
```

### 4. 字段说明

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `style_type` | ENUM | 样式类型 | `'paper'`, `'font'`, `'border'` |
| `style_name` | VARCHAR(50) | 样式名称 | `'古典信纸'` |
| `style_value` | VARCHAR(255) | 样式值 | `'classic'` |
| `preview_url` | VARCHAR(255) | 预览图片URL | `'/uploads/styles/classic-paper.jpg'` |
| `is_active` | BOOLEAN | 是否启用 | `1` (启用) 或 `0` (禁用) |

### 5. 图片URL格式

- **相对路径**（推荐）：`/uploads/styles/文件名.jpg`
- **绝对URL**：`https://example.com/images/classic-paper.jpg`

---

## 📋 方法三：修改初始化脚本

### 1. 编辑 `scripts/init-data.js`

在样式配置数组中添加你的样式：

```javascript
const styles = [
  // ... 现有样式 ...
  { 
    type: 'paper', 
    name: '我的自定义信纸', 
    value: 'my-custom-paper', 
    preview_url: '/uploads/styles/my-custom-paper.jpg' 
  },
  // ... 更多样式 ...
];
```

### 2. 将图片放到指定位置

确保图片文件在 `uploads/styles/` 目录下：
```
uploads/styles/
└── my-custom-paper.jpg
```

### 3. 运行初始化脚本

```bash
node scripts/init-data.js
```

**注意**：如果数据已存在，脚本会跳过初始化。需要先清空相关表或修改脚本逻辑。

---

## 📋 方法四：批量导入SQL

### 1. 准备图片文件

将所有图片复制到 `uploads/styles/` 目录。

### 2. 创建SQL文件

创建 `styles-import.sql`：

```sql
USE cross_time_post;

-- 插入信纸样式
INSERT INTO style_configs (style_type, style_name, style_value, preview_url, is_active) VALUES
('paper', '古典信纸', 'classic', '/uploads/styles/classic-paper.jpg', 1),
('paper', '现代简约', 'modern', '/uploads/styles/modern-paper.jpg', 1),
('paper', '浪漫花纹', 'romantic', '/uploads/styles/romantic-paper.jpg', 1);

-- 插入字体样式
INSERT INTO style_configs (style_type, style_name, style_value, preview_url, is_active) VALUES
('font', '楷体字体', 'kaiti', '/uploads/styles/kaiti-font.jpg', 1),
('font', '行书字体', 'xingshu', '/uploads/styles/xingshu-font.jpg', 1);

-- 插入边框样式
INSERT INTO style_configs (style_type, style_name, style_value, preview_url, is_active) VALUES
('border', '古典边框', 'classic', '/uploads/styles/classic-border.jpg', 1),
('border', '简约边框', 'simple', '/uploads/styles/simple-border.jpg', 1);
```

### 3. 执行SQL文件

```bash
mysql -u root -p cross_time_post < styles-import.sql
```

---

## 🎨 图片要求

### 文件格式
- 支持：`.jpg`, `.jpeg`, `.png`, `.gif`, `.webp`

### 文件大小
- 建议：不超过 5MB
- 后端限制：最大 5MB

### 推荐尺寸
- **信纸样式**：800x600 或更大（宽高比 4:3 或 16:9）
- **字体样式**：400x300 或更大（展示字体效果）
- **边框样式**：400x300 或更大（展示边框效果）

### 图片内容建议
- **信纸样式**：淡雅背景色，可添加纹理，确保文字清晰可读
- **字体样式**：展示不同字体的文字效果示例
- **边框样式**：展示边框的视觉效果

---

## 🔍 验证样式是否添加成功

### 1. 检查数据库

```sql
SELECT * FROM style_configs WHERE style_type = 'paper';
```

### 2. 检查API

```bash
curl http://localhost:3000/api/styles?type=paper
```

### 3. 在前端查看

1. 登录网站
2. 进入写信页面
3. 查看样式选择下拉框，应该能看到新添加的样式

---

## ⚠️ 注意事项

1. **图片路径**
   - 相对路径必须以 `/uploads/styles/` 开头
   - 确保图片文件实际存在于服务器上

2. **样式值唯一性**
   - 同一类型下，`style_value` 应该唯一
   - 如果重复，后添加的会覆盖先添加的（取决于你的逻辑）

3. **图片访问**
   - 确保后端静态文件服务已配置（`server.js` 中已配置）
   - 图片可以通过 `http://localhost:3000/uploads/styles/文件名` 访问

4. **数据库字符集**
   - 确保数据库使用 UTF-8 字符集，以支持中文样式名称

---

## 📝 示例：完整添加流程

### 示例1：添加一个古典信纸样式

```bash
# 1. 准备图片
# 将 classic-paper.jpg 放在项目根目录的 images/ 文件夹

# 2. 运行脚本
node scripts/add-style.js paper "古典信纸" classic ./images/classic-paper.jpg

# 3. 验证
curl http://localhost:3000/api/styles?type=paper
```

### 示例2：批量添加多个样式

```bash
# 添加多个信纸样式
node scripts/add-style.js paper "古典信纸" classic ./images/classic.jpg
node scripts/add-style.js paper "现代简约" modern ./images/modern.jpg
node scripts/add-style.js paper "浪漫花纹" romantic ./images/romantic.jpg

# 添加字体样式
node scripts/add-style.js font "楷体" kaiti ./images/kaiti.jpg
node scripts/add-style.js font "行书" xingshu ./images/xingshu.jpg

# 添加边框样式
node scripts/add-style.js border "古典边框" classic ./images/classic-border.jpg
node scripts/add-style.js border "简约边框" simple ./images/simple-border.jpg
```

---

## 🐛 常见问题

### Q: 图片上传后无法显示？
A: 检查：
1. 图片文件是否在 `uploads/styles/` 目录下
2. 数据库中的 `preview_url` 是否正确
3. 后端静态文件服务是否正常（访问 `http://localhost:3000/uploads/styles/文件名` 测试）

### Q: 样式添加后前端看不到？
A: 检查：
1. 前端是否刷新了样式列表（可能需要刷新页面）
2. `is_active` 字段是否为 1（启用状态）
3. API 返回的数据是否正确

### Q: 脚本运行报错？
A: 检查：
1. 图片路径是否正确
2. 图片文件是否存在
3. 数据库连接是否正常
4. Node.js 模块是否已安装（`npm install`）

---

## 📚 相关文件

- **脚本文件**：`scripts/add-style.js`
- **初始化脚本**：`scripts/init-data.js`
- **数据库结构**：`schema.sql`
- **上传路由**：`routes/upload.js`
- **样式API**：`routes/styles.js`

