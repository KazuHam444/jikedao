# 添加人物选项指南

本指南说明如何在写信页面添加更多人物选项，并同步到协作者。

## 方法一：通过管理员后台添加 + Git同步（推荐）

### ⚠️ 重要说明

**通过管理员后台添加的人物数据存储在数据库中，Git推送代码不会同步数据库数据！**

因此需要以下步骤来同步给协作者：

### 步骤：

#### 1. 添加人物（你的环境）

1. **登录管理员后台**
   - 访问：`http://localhost:5173/admin/login`
   - 使用管理员账号登录

2. **进入历史人物管理**
   - 点击左侧菜单的"历史人物管理"

3. **添加新人物**
   - 点击"添加历史人物"按钮
   - 填写以下信息：
     - **姓名**：人物名称（必填）
     - **时代**：所属时代/类别（必填），例如："唐朝"、"神话"、"现代"、"未来"、"动漫"等
     - **简介**：人物简介（可选）
     - **头像URL**：头像图片链接（可选）
     - **AI提示词**：用于AI生成回信的提示词模板（可选，建议填写）

4. **保存**
   - 点击"保存"按钮
   - 新人物会立即出现在写信页面的选择列表中

#### 2. 导出人物数据（你的环境）

添加完所有人物后，运行导出命令：

```bash
npm run export-figures
```

这会生成 `figures-export.json` 文件，包含所有人物数据。

#### 3. 提交到Git

将 `figures-export.json` 文件提交到Git仓库：

```bash
git add figures-export.json
git commit -m "添加新人物数据"
git push
```

#### 4. 协作者同步（协作者环境）

协作者拉取代码后，运行导入命令：

```bash
git pull
npm run import-figures
```

这样协作者就能获得所有新添加的人物了！

### 优点：
- ✅ 操作简单，通过界面添加
- ✅ 数据文件纳入Git管理，版本可控
- ✅ 协作者一键同步，无需手动操作

---

## 方法二：批量添加（适合大量添加）

### 步骤：

1. **编辑脚本文件**
   - 打开 `scripts/add-figures.js`
   - 在 `figuresToAdd` 数组中添加要添加的人物信息

2. **运行脚本**
   ```bash
   npm run add-figures
   ```
   或
   ```bash
   node scripts/add-figures.js
   ```

3. **查看结果**
   - 脚本会自动检查重复，跳过已存在的人物
   - 显示添加成功和跳过的数量

### 示例：添加人物到脚本

编辑 `scripts/add-figures.js`，在 `figuresToAdd` 数组中添加：

```javascript
const figuresToAdd = [
  // ... 现有的人物 ...
  
  // 添加新人物
  {
    name: '角色名称',
    era: '时代/类别',
    biography: '角色简介',
    avatar_url: '头像URL（可选）',
    prompt_template: 'AI提示词模板（可选）'
  },
  // ... 更多人物 ...
]
```

---

## 方法三：快速同步脚本（最简单）

如果你已经通过管理员后台添加了人物，想要同步给协作者，可以使用这个一键脚本：

### 步骤：

#### 1. 运行同步脚本（你的环境）

```bash
npm run sync-figures
```

这个脚本会：
- ✅ 自动导出所有人物数据到 `figures-export.json`
- ✅ 显示下一步操作提示

#### 2. 提交到Git

```bash
git add figures-export.json
git commit -m "更新人物数据"
git push
```

#### 3. 协作者同步

协作者拉取代码后运行：

```bash
git pull
npm run import-figures
```

---

## 方法四：导出/导入（手动操作）

### 场景：你已经添加了一些人物，想要同步给协作者

### 步骤：

#### 1. 导出人物数据（在你的环境中）

```bash
npm run export-figures
```

或

```bash
node scripts/export-figures.js
```

这会生成 `figures-export.json` 文件，包含所有人物数据。

#### 2. 将文件发送给协作者

将生成的 `figures-export.json` 文件发送给协作者（可以通过Git、邮件等方式）。

#### 3. 协作者导入数据

协作者将 `figures-export.json` 文件放到项目根目录，然后运行：

```bash
npm run import-figures
```

或

```bash
node scripts/import-figures.js
```

也可以指定文件路径：

```bash
node scripts/import-figures.js /path/to/figures-export.json
```

### 导入行为：
- ✅ 如果人物不存在，会添加新人物
- 🔄 如果人物已存在（同名同时代），会更新现有记录
- ⏭️ 不会删除已有的人物

---

## 人物字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `name` | 字符串 | ✅ | 人物名称，例如："李白"、"孙悟空" |
| `era` | 字符串 | ✅ | 时代/类别，例如："唐朝"、"神话"、"现代"、"动漫" |
| `biography` | 文本 | ❌ | 人物简介，用于展示 |
| `avatar_url` | 字符串 | ❌ | 头像图片URL |
| `prompt_template` | 文本 | ❌ | AI回信提示词模板，建议填写以获得更好的AI回复效果 |
| `is_active` | 布尔值 | ❌ | 是否启用，默认为 `true`（启用） |

---

## 注意事项

1. **数据库共享**
   - 所有人物数据存储在共享数据库中
   - 一旦添加，所有用户都能看到和使用
   - 不需要单独为每个用户添加

2. **时代/类别字段**
   - `era` 字段不限于历史时代
   - 可以是："唐朝"、"神话"、"现代"、"未来"、"动漫"、"游戏"、"小说" 等任何类别

3. **AI提示词**
   - 如果填写了 `prompt_template`，AI会根据这个模板生成回信
   - 建议为每个角色填写，以获得更符合角色性格的回信

4. **头像URL**
   - 可以是网络图片URL
   - 也可以是本地图片路径（需要先上传到服务器）

5. **同步到协作者**
   - 如果使用共享数据库，添加后协作者自动可见
   - 如果使用不同数据库，使用导出/导入方法同步

---

## 快速开始示例

### 添加一个虚拟角色：

**通过管理员后台：**
1. 登录管理员后台
2. 进入"历史人物管理"
3. 点击"添加历史人物"
4. 填写：
   - 姓名：`哆啦A梦`
   - 时代：`未来`
   - 简介：`来自22世纪的猫型机器人`
   - AI提示词：`你是哆啦A梦，一只来自22世纪的猫型机器人...`
5. 保存

**通过脚本：**
编辑 `scripts/add-figures.js`，添加后运行 `npm run add-figures`

---

## 常见问题

**Q: 添加后协作者看不到？**
A: 检查是否使用同一个数据库。如果是不同数据库，使用导出/导入方法。

**Q: 如何批量添加大量人物？**
A: 使用方法二（批量添加脚本），在 `figuresToAdd` 数组中添加所有人物信息。

**Q: 可以删除人物吗？**
A: 可以通过管理员后台将人物的 `is_active` 设置为 `false`（禁用），这样人物不会出现在选择列表中，但已发送的信件不受影响。

**Q: 如何修改已有的人物信息？**
A: 在管理员后台的"历史人物管理"页面，点击"编辑"按钮进行修改。

---

## 相关文件

- `scripts/add-figures.js` - 批量添加脚本
- `scripts/export-figures.js` - 导出数据脚本
- `scripts/import-figures.js` - 导入数据脚本
- `frontend/src/views/admin/Figures.vue` - 管理员后台界面
- `routes/admin.js` - 管理员API路由
- `routes/figures.js` - 人物列表API

